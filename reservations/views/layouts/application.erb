<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type" />
    <meta name=viewport content='width=device-width,initial-scale=1' />
    <title>Reservations 2.0</title>
    <%= javascript_include_tag 'jquery', 'bootstrap.min', 'jquery-ujs', 'application' %>
    <%= stylesheet_link_tag 'bootstrap', 'bootstrapXL', 'bootstrap-theme', 'application' %>
  </head>
  <body>
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container-fluid">
        <ul class="nav navbar-nav navbar-left pull-left">
          <%= link_to 'Reservations', url(:reservation, :index), :class => 'navbar-brand', :title => 'Reservations' %>
        </ul>
        <p class="navbar-text navbar-right pull-right">@<a class="navbar-link"><%= @current_user.display_name %></a></p>
      </div>
      <hr/>
      <div class="container row">
        <ul class="nav navbar-nav">
          <% @recreations.each do |recreation| %>
            <li class=navbar-module>
              <%= link_to recreation.name, "##{recreation.name.gsub(' ', '_')}" %>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>

    <div data-control="alert-container">
      <%= {:error => 'danger', :warning => 'warning', :success => 'success', :notice => 'info'}.map { |type, class_name| flash_tag(type, :class => "alert alert-#{class_name} fade in", :bootstrap => true) }.join.html_safe %>
    </div>

    <div data-control="main" class='container-fluid main'>
      <div class='main-wrapper'>
        <div class="row"><%= yield %></div>
        <div class='main-wrapper-push'></div>
      </div>
    </div>

    <footer>
      <div data-control="footer" class='footer-wrapper container-fluid'>
        <p class='pull-left'>Copyright &copy; 2017 Michał Żarłok</p>
      </div>
    </footer>
  </body>
</html>