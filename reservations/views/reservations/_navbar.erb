<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <ul class="nav navbar-nav pull-left">
      <li class="dropdown">
        <a class="navbar-brand" id="main-menu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" title="Main Menu">
          Reservations
          <span class="caret"></span>
        </a>
        <ul class="dropdown-menu" aria-labelledby="main-menu" data-control="main-menu">
          <li>
            <a href="<%= webcal_url(:calendars, :user, :id => @current_user.id, :format => :ics) %>"><span class="glyphicon glyphicon-calendar" aria-hidden="true"></span> Subscribe to web calendar</a>
          </li>
        </ul>
      </li>
    </ul>
    <p class="navbar-text navbar-right pull-right">
      @<a class="navbar-link" data-control="user-name" data-type="text" data-url="<%= Recreations::Base.url(:user, :update) %>" data-placement="bottom" data-showbuttons="false" data-send="always"><%= @current_user.display_name %></a>
    </p>
  </div>
  <hr/>
  <div class="container row">
    <ul class="nav navbar-nav navbar-links">
      <% @recreations.each do |recreation| %>
          <li class=navbar-module>
            <%= link_to recreation.name, "##{recreation.name.gsub(' ', '_')}" %>
          </li>
      <% end %>
    </ul>
  </div>
</nav>
