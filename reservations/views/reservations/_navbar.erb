<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <ul class="nav navbar-nav pull-left">
      <li class="dropdown">
        <a class="navbar-brand" id="main-menu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" title="Main Menu">
          Reservations
          <span class="caret"></span>
        </a>
        <ul class="dropdown-menu" aria-labelledby="main-menu" data-control="main-menu">
          <li>
            <a href="<%= webcal_url(:calendars, :user, :id => @current_user.id, :format => :ics) %>">
              <span class="glyphicon glyphicon-calendar" aria-hidden="true"></span>Subscribe to web calendar
            </a>
          </li>
          <li>
            <a class="flex-row" data-control="menu-settings">
              <span class="glyphicon glyphicon-comment" aria-hidden="true"></span><label for="spark-bot-notifications">Allow Spark bot notifications</label>
              <div class="material-switch">
                <input id="spark-bot-notifications" name="spark-integration" type="checkbox" <%= @current_user.spark_integration ? 'checked' : '' %> />
                <label for="spark-bot-notifications" class="label-success"></label>
              </div>
            </a>
          </li>
        </ul>
      </li>
    </ul>
    <p class="navbar-text navbar-right pull-right">
      @<a class="navbar-link editable editable-click" data-toggle="popover" role="button" tabindex="0" data-control="user-name" data-html="true" data-placement="bottom" data-user-display-name="<%= @current_user.display_name %>" data-user-email="<%= @current_user.email %>"><%= @current_user.display_name %></a>
    </p>
  </div>
  <hr/>
  <div class="container row">
    <ul class="nav navbar-nav navbar-links">
      <% @recreations.each do |recreation| %>
          <li class="navbar-module">
            <a href="<%= url_for "##{permalink_for(recreation.name)}" %>"><i class="flaticon-<%= recreation.name.gsub(' ', '_') %>"></i><span><%= recreation.name %></span></a>
          </li>
      <% end %>
    </ul>
  </div>
</nav>
<div data-control="current-date"><%= @date.strftime("%a, %F") %></div>
