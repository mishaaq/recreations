<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:reservations, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:reservations, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :reservation, url(:reservations, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'reservations/form', :locals => { :f => f } %>
 <% end %>
</div>
