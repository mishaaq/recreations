<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:reservations, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:reservations, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:reservations, :edit, :id => @reservation.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :reservation, url(:reservations, :update, :id => @reservation.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'reservations/form', :locals => { :f => f } %>
  <% end %>
</div>
